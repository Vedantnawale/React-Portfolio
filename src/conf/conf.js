const conf = {
    emailService: String(import.meta.env.VITE_EMAILJS_SERVICE_ID),
    emailTemp: String(import.meta.env.VITE_EMAILJS_SERVICE_ID),
    emailKey: String(import.meta.env.VITE_EMAILJS_SERVICE_ID)
}

export default conf;